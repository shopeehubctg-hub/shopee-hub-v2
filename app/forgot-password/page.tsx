"use client";
import{FormEvent,useState}from"react";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[whatsappUrl,setWhatsappUrl]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent){event.preventDefault();setLoading(true);setMessage("");setWhatsappUrl("");try{const response=await fetch("/api/auth/recover",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const data=await response.json();if(!response.ok){setMessage(data.error||"Unable to process this request.");return}setWhatsappUrl(data.whatsappUrl);setMessage("Your portal access is active. Contact the Shopee Hub Specialist to receive the temporary password.")}catch{setMessage("Unable to process this request. Please contact a Shopee Hub Specialist.")}finally{setLoading(false)}}return <main className="login-shell"><section className="login-card"><p className="kicker">PASSWORD RESET</p><h1>Reset your password</h1><p>Enter your portal email. We will verify your access before you contact our specialist.</p><form onSubmit={submit}><label>Email address<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><button disabled={loading}>{loading?"Checking…":"Check portal access"}</button></form>{message&&<p className={whatsappUrl?"login-message":"login-message error"}>{message}</p>}{whatsappUrl&&<><a className="whatsapp-reset-button" href={whatsappUrl} target="_blank" rel="noreferrer">Contact Specialist on WhatsApp</a><a href={`/reset-password?email=${encodeURIComponent(email.trim().toLowerCase())}`}>I already have the temporary password</a></>}<a href="/login">Back to sign in</a></section></main>}
