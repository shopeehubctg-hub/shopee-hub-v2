"use client";
import{FormEvent,useEffect,useState}from"react";
export default function ResetPassword(){const[email,setEmail]=useState("");const[temporaryPassword,setTemporaryPassword]=useState("");const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);useEffect(()=>setEmail(new URLSearchParams(window.location.search).get("email")||""),[]);async function submit(event:FormEvent){event.preventDefault();setMessage("");if(password!==confirm){setMessage("Passwords do not match");return}setLoading(true);try{const response=await fetch("/api/auth/password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,temporaryPassword,password})});const data=await response.json();if(!response.ok){setMessage(data.error||"Unable to reset password");return}window.location.replace("/login?password_reset=success")}catch{setMessage("Unable to reset password. Please contact a Shopee Hub Specialist.")}finally{setLoading(false)}}return <main className="login-shell"><section className="login-card"><p className="kicker">SECURE PORTAL</p><h1>Choose a new password</h1><p>Enter the temporary password provided by the Shopee Hub Specialist.</p><form onSubmit={submit}><label>Email address<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Temporary password<input type="password" autoComplete="one-time-code" required value={temporaryPassword} onChange={e=>setTemporaryPassword(e.target.value)}/></label><label>New password<input type="password" autoComplete="new-password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirm password<input type="password" autoComplete="new-password" minLength={8} required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button disabled={loading}>{loading?"Saving…":"Save new password"}</button></form>{message&&<p className="login-message error">{message}</p>}<a href="/forgot-password">Back</a></section></main>}
